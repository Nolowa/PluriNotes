#include "versionsdockview.h"

VersionsDockView::VersionsDockView(Database const * database, NoteFrameView const * noteview, QWidget *parent) : QWidget(parent), database(database), noteview(noteview)
{

}
