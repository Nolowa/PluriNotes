#ifndef VERSION_H
#define VERSION_H
#include "notesmanager.h"
#include <sqlite3.h>
/*
class version
{
    static int rc;
public:
    version();
    static void connectBd();
    //static void closeBd(){sqlite3_close(db);}
};

void createTables();
*/

#endif // VERSION_H
